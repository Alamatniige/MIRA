# TASK 1 #

- We need to adjust the asset's model since i added new table and fields at the database. Probably we need to adjust the handler as well for this new table.

# TARGET FILE : [`api\v1\assets\model.go`] & [`api\v1\assets\handler.go`]

assets db: 

create table public.assets (
  id uuid not null default gen_random_uuid (),
  "assetName" text not null,
  "assetType" bigint null,
  "serialNumber" text not null,
  specification text not null,
  "currentStatus" text not null,
  "createdAt" timestamp without time zone null,
  "isAssigned" boolean null default false,
  room bigint null,
  floor bigint null,
  constraint assets_pkey primary key (id),
  constraint assets_assetType_fkey foreign KEY ("assetType") references "assetType" (id),
  constraint assets_floor_fkey foreign KEY (floor) references "assetFloor" (id),
  constraint assets_room_fkey foreign KEY (room) references "assetRoom" (id),
  constraint fk_assets_asset_type_rel foreign KEY ("assetType") references "assetType" (id)
) TABLESPACE pg_default;

assetType db: 
create table public."assetType" (
  id bigint generated by default as identity not null,
  name text null,
  created_at timestamp with time zone not null default now(),
  constraint assetType_pkey primary key (id)
) TABLESPACE pg_default;

assetFloor db: 
create table public.assetFloor (
  id bigint generated by default as identity not null,
  name text null,
  created_at timestamp with time zone not null default now(),
  constraint assetFloor_pkey primary key (id)
) TABLESPACE pg_default;

assetRoom db: 
create table public.assetRoom (
  id bigint generated by default as identity not null,
  name text null,
  created_at timestamp with time zone not null default now(),
  constraint assetRoom_pkey primary key (id)
) TABLESPACE pg_default;

Those are the new table as well as the fields that i added to the assets table. 