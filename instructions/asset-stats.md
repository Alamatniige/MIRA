# TASK #

Asset Page Stats Card.

Target File : [`web\components\asset\AssetRegistry.tsx`]

Instruction : 

We need to create the status logics for this page in order to show real-data output. I don't know if we need to create it in the hooks or in the server so we can just directly call it.

Note: 

If there is no existing fields to call of, try to add an || logic so we can implement the logic for that later on when the field is created.

Might aswell fixed those lints errors.

Mentioned Files :
[`web\hooks\useAssets.ts`]
[`api\v1\assets\handler.go`]