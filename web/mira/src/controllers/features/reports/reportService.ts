import type { UtilizationData, InventorySummaryRow } from "@/types/report.types";

export async function fetchUtilizationReport(): Promise<UtilizationData[]> {
  // Placeholder - integrate with API
  return [];
}

export async function fetchInventorySummary(): Promise<InventorySummaryRow[]> {
  // Placeholder - integrate with API
  return [];
}
